# =============================================================================
# GMQ 580 — Séance 06 : dépendances Python (installation pip)
#
# AVERTISSEMENT : les paquets géospatiaux (geopandas, rasterio, cartopy)
# reposent sur des bibliothèques système C (GDAL, GEOS, PROJ).
# L'installation pip peut échouer sur Windows/macOS sans environnement
# préconfiguré. Préférer l'approche conda (voir environment.yml).
# =============================================================================

# --- Données vecteur ---
geopandas>=1.0
pyogrio>=0.7          # moteur de lecture .gpkg rapide (utilisé par geopandas)
shapely>=2.0          # géométries (dépendance automatique de geopandas)

# --- Données raster ---
rasterio>=1.3

# --- Calcul numérique ---
numpy>=1.26
pandas>=2.1

# --- Cartographie statique ---
matplotlib>=3.8
mapclassify>=2.6
contextily>=1.4
cartopy>=0.22

# --- Cartes interactives ---
folium>=0.17
plotly>=5.18

# --- Tableaux de bord ---
dash>=2.16
streamlit>=1.35
streamlit-folium>=0.22

# --- Rapport Word ---
python-docx>=1.0

# --- Utilitaires ---
gdown>=5.0            # téléchargement Google Drive
pyyaml>=6.0           # lecture config.yaml
